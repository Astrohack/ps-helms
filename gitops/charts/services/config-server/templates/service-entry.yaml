apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: config-repo-external
spec:
  hosts:
    - github.com
  ports:
    - number: 443
      name: https
      protocol: HTTPS
  resolution: DNS