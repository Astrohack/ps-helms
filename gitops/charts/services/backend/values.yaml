domains:
  backend: "backend.dev.localhost"

replicaCount: 1

image:
  repository: hashicorp/http-echo
  tag: "1.0.0"
  pullPolicy: IfNotPresent


service:
  type: ClusterIP
  port: 8080

config:
  dbHost: postgres-rw.database.svc.cluster.local
  dbName: app_db
  message: "Hello from the Enterprise Backend Service!"
