apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: openrouter-llm-external
spec:
  hosts:
    - openrouter.ai
  ports:
    - number: 443
      name: https
      protocol: HTTPS
  resolution: DNS