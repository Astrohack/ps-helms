apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: openrouter-llm-external
spec:
  hosts:
    - openrouter.ai
  ports:
    - number: 443
      name: https
      protocol: HTTPS
  resolution: DNS
---
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: haveibeenpwned-external
spec:
  hosts:
    - api.pwnedpasswords.com
  ports:
    - number: 443
      name: https
      protocol: HTTPS
  resolution: DNS
